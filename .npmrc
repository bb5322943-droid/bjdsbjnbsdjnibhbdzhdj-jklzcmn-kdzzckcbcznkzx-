# npm configuration
# Enable peer dependencies auto-install
legacy-peer-deps=true

# pnpm: build scripts ruxsati
# @swc/core va esbuild native binary uchun build script ishlashi kerak
ignore-scripts=false
